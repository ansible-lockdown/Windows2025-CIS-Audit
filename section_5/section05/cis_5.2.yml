---

{{ if and (ne .Vars.win25cis_system_role "domaincontroller") .Vars.win25cis_level_2 }}
  {{ if .Vars.win25cis_rule_5_2 }}
    {{ if ne .Vars.win25cis_system_role "domaincontroller" }}
      {{ if ne .Vars.win25cis_system_role "standalone" }}
file:
  win25cis_5_2_spooler:
    title: "5.2 | Ensure 'Print Spooler (Spooler)' is set to 'Disabled' (MS only) | Spooler"
    path: '{{ .Vars.win25cis_snapshot_dir }}\Service.Spooler'
    exists: true
    contents:
      - '/^(Disabled|NotInstalled)$/'
    meta:
      memberserver: 2
      server: 2
      CIS_ID:
        - 5.2
      {{ end }}
    {{ end }}
  {{ end }}
{{ end }}
